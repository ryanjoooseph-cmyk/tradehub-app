# Agent Job (Issue #18693)

## Title
[P0] Build metadata: always show Commit + Version time

## Objective
Job-ID: P0-foundation-002
Priority: P0

Ensure Commit + Version time never show '-':
- emit public/build.json {sha,builtAt} during build/CI
- /api/version reads it (fallback env)
- UI displays it
Acceptance:
- prod shows sha + builtAt
