Here's a minimal Next.js page component for the specified path `app/jobs/page.tsx`:

```tsx
// app/jobs/page.tsx

import React from 'react';

const PageJobs: React.FC = () => {
  return (
    <div>
      <h1>Job Listings</h1>
      <p>Placeholder content for job listings will go here.</p>
    </div>
  );
};

export default PageJobs;
```

This component includes a heading and a placeholder paragraph, ready for further development.