Here's a minimal Next.js page component for the specified path `app/jobs/page.tsx`:

```tsx
// app/jobs/page.tsx

import React from 'react';

const PageJobs: React.FC = () => {
  return (
    <div>
      <h1>Jobs</h1>
      <p>This is a placeholder for job listings.</p>
    </div>
  );
};

export default PageJobs;
```

This component renders a heading and a placeholder paragraph, following the structure you requested.