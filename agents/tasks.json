{
  "queue": [
    {
      "name": "auth_frontend_407",
      "label": "automerge",
      "paths": ["app/login/**", "app/register/**", "app/logout/**", "lib/supabase/**", "app/layout.tsx"],
      "plan": "Add tiny useSession hook (client) and show Login/Logout in nav; redirect links only (no middleware)."
    },
    {
      "name": "auth_hardening_407b",
      "label": "automerge",
      "paths": ["app/(components)/**", "lib/**"],
      "plan": "Add AuthGate client wrapper to hide protected sections when not signed in."
    },
    {
      "name": "tenants_ui_408",
      "label": "automerge",
      "paths": ["app/tenants/**", "lib/**"],
      "plan": "Wire tenantId from localStorage into fetch headers; highlight active tenant in nav."
    },
    {
      "name": "tenants_backend_stub_408b",
      "label": "automerge",
      "paths": ["app/api/**", "lib/**"],
      "plan": "Plumb optional `x-tenant-id` header through /api/jobs, /api/quotes, /api/invoices (no schema changes)."
    },
    {
      "name": "admin_dashboard_409",
      "label": "automerge",
      "paths": ["app/admin/**", "lib/ui/**"],
      "plan": "Add quick links + recent activity list by calling existing APIs with ?limit=10."
    },
    {
      "name": "nav_links_406b",
      "label": "automerge",
      "paths": ["app/layout.tsx", "app/(components)/**"],
      "plan": "Add nav items: Clients, Jobs, Quotes, Invoices, Payments, Disputes, Tenants, Admin."
    }
  ]
}
{
  "queue": [
    {
      "name": "invoices_frontend_401",
      "label": "automerge",
      "paths": ["app/invoices/**", "lib/ui/**"],
      "plan": "Improve invoices table: pagination, client name lookup, inline status edit."
    },
    {
      "name": "invoices_backend_401",
      "label": "automerge",
      "paths": ["app/api/invoices/**", "lib/validators/**"],
      "plan": "Add server-side validation & pagination params (?limit,?offset)."
    },
    {
      "name": "payments_frontend_402",
      "label": "automerge",
      "paths": ["app/payments/**", "lib/ui/**"],
      "plan": "Add filters (status/method) and link to related invoice."
    },
    {
      "name": "payments_backend_402",
      "label": "automerge",
      "paths": ["app/api/payments/**", "lib/validators/**"],
      "plan": "Add idempotency key header support and server-side sorting."
    },
    {
      "name": "disputes_frontend_403",
      "label": "automerge",
      "paths": ["app/disputes/**", "lib/ui/**"],
      "plan": "Add reason textarea, quick status buttons (open/needs-info/resolved)."
    },
    {
      "name": "disputes_backend_403",
      "label": "automerge",
      "paths": ["app/api/disputes/**", "lib/validators/**"],
      "plan": "Add audit columns (updated_by, updated_at) and basic status transitions."
    },
    {
      "name": "quotes_pdf_404",
      "label": "automerge",
      "paths": ["app/api/quotes/**", "lib/pdf/**"],
      "plan": "Add /api/quotes/:id/pdf that returns a minimal PDF; no deps, server-rendered HTML->PDF stub."
    },
    {
      "name": "invoices_mark_paid_405",
      "label": "automerge",
      "paths": ["app/api/invoices/**", "app/invoices/**"],
      "plan": "Add POST /api/invoices/:id/mark-paid and a button in UI."
    },
    {
      "name": "layout_nav_406",
      "label": "automerge",
      "paths": ["app/layout.tsx", "app/(components)/**"],
      "plan": "Add top nav links to Clients, Jobs, Quotes, Invoices, Payments, Disputes."
    }
  ]
}
