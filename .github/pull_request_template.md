### What changed
-

### Scope check
- [ ] UI-only (app/components only)  -> eligible for auto-merge
- [ ] Touches schema/deps/config     -> label added: allow-schema / allow-deps / allow-config

### Proof
- [ ] `next build` passes locally or in CI
