name: Enable PR Auto-merge (DISABLED - manual only)

on:
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled. Use enable-automerge.yml (pull_request_target) instead."
