name: safe-automerge
on:
  pull_request_target:
    types: [opened, reopened, synchronize, labeled, ready_for_review]
permissions:
  contents: read
  pull-requests: read

jobs:
  present:
    runs-on: ubuntu-latest
    steps:
      - run: echo "gate present"
