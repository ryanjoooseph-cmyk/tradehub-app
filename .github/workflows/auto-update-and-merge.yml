name: auto-update-and-merge (manual only)

on:
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled automation. Run manually via workflow_dispatch if needed."
