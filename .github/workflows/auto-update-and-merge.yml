name: auto-update-and-merge (manual only)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Manual only. Disabled to prevent automated failures."
