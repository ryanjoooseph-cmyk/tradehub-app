name: required-check
on:
  pull_request:
    types: [opened, synchronize, reopened, labeled, ready_for_review]
permissions:
  contents: read
jobs:
  gate:
    runs-on: ubuntu-latest
    steps:
      - name: succeed fast
        run: echo ok
