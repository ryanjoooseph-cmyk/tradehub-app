name: agent-required-check
on:
  pull_request:
    types: [opened, synchronize, reopened, labeled]
permissions:
  contents: read
jobs:
  required:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - run: echo ok
