name: agent-ci
on:
  pull_request:
    branches: [ main ]
  push:
    branches: [ agent-* ]
permissions:
  contents: read
  statuses: write
jobs:
  agent-ci:
    runs-on: ubuntu-latest
    steps:
      - run: echo ok
