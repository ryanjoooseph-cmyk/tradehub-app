name: agent-ci
on:
  pull_request_target:
    types: [opened, synchronize, reopened]
jobs:
  agent-ci:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - run: echo ok
