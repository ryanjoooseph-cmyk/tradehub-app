name: validate-agent-pr
on:
  pull_request_target:
    types: [opened, reopened, synchronize, labeled, edited]
permissions:
  contents: read
  pull-requests: write
jobs:
  guard:
    runs-on: ubuntu-latest
    steps:
      - run: echo ok
